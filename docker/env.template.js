// Template rendered at container start by docker/entrypoint.sh (envsubst).
// ${VITE_API_URL} is replaced with the container's VITE_API_URL environment variable.
window.__ENV__ = {
  VITE_API_URL: "${VITE_API_URL}",
};
